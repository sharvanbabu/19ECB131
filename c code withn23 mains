#include <stdio.h>

int main() {
    int a;
    printf("Enter an integer (positive or negative): ");
    scanf("%d", &a);
    if (a > 0) {
      printf("The nubmber %d is positive", a);
      if (a %2 == 0)
        printf("\nThe number is divisible by 2");
      else
        printf("\nThe number is not divisible by 2");
    }
    else if (a < 0)
      printf("The number %d is not positive", a);
    else
        printf("The number %d is zero", a);
    
    return 0;
}


Enter an integer (positive or negative): 2
The nubmber 2 is positive
The number is divisible by 2

=== Code Execution Successful =


Enter an integer (positive or negative): 3
The nubmber 3 is positive
The number is not divisible by 2

=== Code Execution Successful ===


